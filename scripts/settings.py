import os

# Use environment variable instead of hardcoding API key
OPENAI_API_KEY = os.getenv("OPENAI_API_KEY")

MAP_WIDTH = 20  # Width of the map (number of tiles horizontally)
MAP_HEIGHT = 15  # Height of the map (number of tiles vertically)


SCREEN_WIDTH = 800
SCREEN_HEIGHT = 600
